


const createCard = (src, span, title, desc) => {

    return `
    <img class="object-cover" src="assets/img/download.webp" alt="" srcset="">
    <span>${span}</span>
    <h2>${title}</h2>
    <hr>
    <p>${desc}</p>`;
}